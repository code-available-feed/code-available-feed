# Generated feed files (docs/arxiv/...) live on gh-pages, not on main.
# scripts/deploy_orphan.sh removes this file before committing to gh-pages,
# so these ignore rules do not affect what is deployed.
arxiv/
